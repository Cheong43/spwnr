#!/usr/bin/env bash

cat <<'EOF'
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "Spwnr is available. Use /using-spwnr as the router: small bounded work may use /spwnr-do; non-trivial work should plan with /spwnr-plan, then execute approved plans with /spwnr-task; registry/readiness recovery uses /spwnr-worker-audit. Load the relevant skill before acting and keep prompts lean: read/edit targeted plan sections, brief agents with concise goals plus plan path/section names, and avoid passing full files or thread history unless required."
  }
}
EOF
