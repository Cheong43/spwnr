#!/usr/bin/env bash

cat <<'EOF'
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "The Spwnr plugin is available. For non-trivial work, prefer /spwnr:plan when you want strategy first and /spwnr:task when you want the full research -> execute -> review loop. Use /spwnr:workers to inspect required worker subagents: general-researcher, general-executor, general-reviewer. Use code-reviewer only when the request is explicitly code-focused."
  }
}
EOF
