#!/usr/bin/env bash

cat <<'EOF'
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "The Spwnr plugin is available. For non-trivial work, start with /spwnr:plan to align and lock the plan first. Planning should load workflow-foundation and workflow-planning with Skill, ask only material clarification questions with AskUserQuestion, track blockers plus approval state with TodoWrite, inspect repo context with Read, and persist the detailed plan to .claude/plans/spwnr-<project-folder-name>-<YYYY-MM-DD>.md with Write or Edit. /spwnr:plan must not create tasks, teams, agents, or worktrees. /spwnr:task uses the same planning gate and only executes after explicit approval, then reads that plan file with Read, appends the approved execution spec with Edit, resolves registry candidates with spwnr resolve-workers, creates exact execution tasks with TaskCreate, validates them with TaskGet and TaskList, updates them with TaskUpdate, orchestrates single-lane, team, or swarm execution, uses TeamCreate, Agent, SendMessage, and TeamDelete only when agent teams are available, requires EnterWorktree and ExitWorktree for swarm writes, and stops with a worker-readiness recovery message instead of silently downgrading when registry resolution or worktree setup fails. Use /spwnr:workers when registry health, installation, injection, or readiness recovery needs a deeper audit."
  }
}
EOF
