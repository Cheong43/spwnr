/**
 * Reserved for future host-specific policy mapping when Spwnr grows a runtime layer.
 * Intentionally left without active behavior in the current release.
 */
export function mapPolicyPlaceholder(): null {
  return null;
}
