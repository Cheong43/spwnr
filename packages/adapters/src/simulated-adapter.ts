/**
 * @deprecated Reserved only as an internal seed if Spwnr grows a managed runtime again.
 * The current product surface does not use a simulated adapter.
 */
export class SimulatedAdapter {
  readonly deprecated = true;
}
