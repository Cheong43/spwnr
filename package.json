{
  "name": "spwnr",
  "private": true,
  "version": "0.3.0",
  "description": "Agent package registry and declarative host injection platform",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "build:registry-portal": "node ./scripts/build-registry-portal.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.9.3",
    "vitest": "^2.0.0"
  }
}
