{
  "name": "orchex",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-runtime Subagent protocol, registry, and runtime platform",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
